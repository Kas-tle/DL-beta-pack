{
  "git": {
    "changelog": "git log --pretty=format:'* %s (%h)' ${from}...${to}"
  },
  "github": {
    "release": true,
    "assets": ["*.zip"],
    "releaseName": "Resource Pack Iteration ${version}",
    "tokenRef": "GITHUB_TOKEN"
  }
}
